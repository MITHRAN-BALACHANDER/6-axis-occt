"use client"
import"./button.css"
import { useState } from "react"
export default function Stop() {
    const [count, setCount] = useState(0)
  
    return (
        <button
        class="button"
    >
        Stop

      </button>
    )
  }
